//#region node_modules/.pnpm/@scaleway+sdk-client@2.6.0/node_modules/@scaleway/sdk-client/dist/internal/interceptors/helpers.js
/**
* Adds an header to a request through an interceptor.
*
* @param key - The header key
* @param value - The header value
* @returns The Request interceptor
*
* @internal
*/
var addHeaderInterceptor = (key, value) => ({ request }) => {
	const clone = request.clone();
	if (value !== void 0) clone.headers.append(key, value);
	return clone;
};

//#endregion
export { addHeaderInterceptor };