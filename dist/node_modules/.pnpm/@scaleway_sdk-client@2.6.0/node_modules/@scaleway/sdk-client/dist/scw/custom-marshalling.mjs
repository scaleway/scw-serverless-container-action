import { isJSONObject } from "../helpers/json.mjs";
import "../vendor/base64/index.mjs";

//#region node_modules/.pnpm/@scaleway+sdk-client@2.6.0/node_modules/@scaleway/sdk-client/dist/scw/custom-marshalling.js
/**
* Unmarshals {@link Money}
*
* @internal
*/
var unmarshalMoney = (data) => {
	if (!isJSONObject(data)) throw new TypeError(`Unmarshalling the type 'Money' failed as data isn't a dictionary.`);
	return {
		currencyCode: data.currency_code,
		nanos: data.nanos,
		units: data.units
	};
};
/**
* Unmarshals {@link ServiceInfo}.
*
* @internal
*/
var unmarshalServiceInfo = (data) => {
	if (!isJSONObject(data)) throw new TypeError(`Unmarshalling the type 'ServiceInfo' failed as data isn't a dictionary.`);
	return {
		description: data.description,
		documentationUrl: data.documentation_url,
		name: data.name,
		version: data.version
	};
};

//#endregion
export { unmarshalMoney, unmarshalServiceInfo };