//#region node_modules/.pnpm/@scaleway+sdk-client@2.6.0/node_modules/@scaleway/sdk-client/dist/scw/locality.js
function toApiLocality(legacy) {
	if (!legacy) return { type: "unspecified" };
	const { zones, regions } = legacy;
	if (zones && zones.length > 0) return {
		type: "zone",
		zones
	};
	if (regions && regions.length > 0) return {
		type: "region",
		regions
	};
	return { type: "global" };
}

//#endregion
export { toApiLocality };